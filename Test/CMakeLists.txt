include_directories(${HEAD_PATH})
set(EXECUTABLE_OUTPUT_PATH ${EXEC_PATH})
link_directories(${LIB_PATH})
add_executable(${NAME_BINARYTREE} BinaryTree_t.cc)
target_link_libraries(${NAME_BINARYTREE} ${MALG})
